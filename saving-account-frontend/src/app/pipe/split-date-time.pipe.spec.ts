import { SplitDateTimePipe } from './split-date-time.pipe';

describe('SplitDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
